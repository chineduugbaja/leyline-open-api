apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "leyline.fullname" . }}-test-connection"
  labels:
    {{- include "leyline.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "leyline.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
